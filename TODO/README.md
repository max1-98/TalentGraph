# TODO — Work Tracker

Outstanding design work, organised by area. Each file tracks one category of
tasks. Items are marked:

- `[ ]` — not started
- `[~]` — in progress
- `[x]` — done

When a task is done, move it to the bottom of its section under a `## Done`
heading so the history is preserved.
